apiVersion: v1
kind: Secret
metadata:
  name: bloomguard-secrets
type: Opaque
stringData:
  db-host: "postgres-service"
  db-user: "bloomguard"
  db-password: "CHANGE_ME_IN_PRODUCTION"
  redis-host: "redis-service"
  redis-password: "CHANGE_ME_IN_PRODUCTION"
